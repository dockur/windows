version: "3.8"

services:
  windows:
    image: dockurr/windows
    container_name: windows
    privileged: true
    environment:
      VERSION: "11"
      DISK_SIZE: "100G"
      RAM_SIZE: "8G"
      CPU_CORES: "4"
      AUDIO: "Y"
      USERNAME: "Andre"
      PASSWORD: "123"
      LANGUAGE: "Español"
    devices:
      - /dev/kvm:/dev/kvm
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
    ports:
      - "8006:8006"
      - "3389:3389/tcp"
      - "3389:3389/udp"
    volumes:
      - ./windows:/storage
      - ./Win11:/shared
    restart: always
    stop_grace_period: 2m
